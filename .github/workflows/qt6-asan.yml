# SPDX-FileCopyrightText: 2024 Klarälvdalens Datakonsult AB, a KDAB Group company <info@kdab.com>
#
# SPDX-License-Identifier: GPL-2.0-only OR GPL-3.0-only

name: Qt6 ASAN_UBSAN (Linux)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    if: "!contains(github.event.pull_request.title, '[only-') || contains(github.event.pull_request.title, '[only-qt6-asan]')"
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os:
          - ubuntu-24.04
        qt-flavor:
          - asan_ubsan

    steps:
      - name: Checkout sources
        uses: actions/checkout@v7

      - name: Install dependencies
        run: |
          sudo apt install mold libspdlog-dev ninja-build libwayland-dev wayland-protocols -y

      - name: Setup Sanitized Qt
        uses: KDABLabs/sanitized-qt-action@v1
        with:
          qt-tag: "v6.12.0-beta4"
          qt-flavor: "${{ matrix.qt-flavor }}"

      # kddw asan build already has ubsan, while Qt has dedicated
      - name: Configure project
        run: cmake -S . -B ./build-dev-asan --preset dev-asan

      - name: Build Project
        run: cmake --build ./build-dev-asan ${{ matrix.preset.build_preset_arg }}

      - name: Run tests
        run: ctest --test-dir ./build-dev-asan --verbose -j 4
        env:
          QT_QUICK_BACKEND: software
